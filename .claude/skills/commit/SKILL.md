## Commit Workflow
1. Run `npx tsc --noEmit` to check for type errors — fix any before proceeding
2. Run `git diff --stat` to review what has changed
3. Create a conventional commit message (feat/fix/chore/docs/test)
4. Stage specific files (not `git add -A`) and commit
